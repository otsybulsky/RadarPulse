• Мета: довести historical NOAA NEXRAD Level II loader до milestone з `archive download` поверх уже готового manifest-first workflow.
• Вже зроблено: реалізовано `archive list` для одного радара і explicit `--all-radars`; генерацію manifest; summary output; JSON persistence; `--max-files`/`--max-bytes`; retry/backoff для S3 listing; deterministic cache path mapping; unit/integration test scaffolding; документацію по loader.
• Перевірено: `dotnet test RadarPulse.sln --no-restore` проходить; 12 tests passed, 1 opt-in integration test skipped; репозиторій чистий по `git status`.
• Важливі файли: `docs/historical-loader.md`, `docs/historical-loader-plan.md`, `src/Presentation/Program.cs`, `src/Application/Archive/IHistoricalArchiveClient.cs`, `src/Infrastructure/Archive/AwsNexradArchiveClient.cs`, `src/Infrastructure/Archive/HistoricalArchiveManifestWriter.cs`, `src/Infrastructure/Archive/NexradCachePathMapper.cs`, `tests/RadarPulse.Tests/Archive/*`.
• Обмеження: network access для live AWS tests тільки opt-in; deprecated bucket `noaa-nexrad-level2` не використовувати; великі дані й manifests під `data/` лишаються поза source control; поточна реалізація ще не читає saved manifest для download.
• Поточна проблема: milestone `archive download` ще не реалізований — немає download command, skip/redownload logic, temp-file write flow, concurrency/cancellation handling і disk/byte guardrails.
• Наступний крок: додати CLI команду `archive download` з обов'язковим `--output`, завантаженням у `data/nexrad/level2/{yyyy}/{MM}/{dd}/{radarId}/{fileName}` і базовою skip/redownload перевіркою за size.
• Критерій готовності: `archive download` завантажує selected manifest entries у deterministic cache, безпечно пропускає valid files, перекачує missing/size-mismatched files, пише через temporary file + final move, поважає cancellation/concurrency/guardrails і покритий тестами та docs.
