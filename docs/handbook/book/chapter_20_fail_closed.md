# Розділ 20: Запобіжник Fail-Closed

Коли у складних, багатопотокових системах обробки великих даних стається збій, вони часто поводяться вкрай небезпечно. Вони продовжують приймати нові завдання від клієнтів, засмічують диски гігабайтами ідентичних логів помилок, безконтрольно накопичують гігабайти неробочих буферів в оперативній пам'яті (Memory Leak) та марно спалюють процесорний час на заздалегідь приречені обчислення. Це класична поведінка патерну **Fail-Open** (відкритий збій).

У системі RadarPulse реалізовано протилежний, безкомпромісний інженерний патерн — **Fail-Closed** (закритий збій, затверджений у вісі `023`). Це наш важкий аварійний заслін, мета якого — миттєво локалізувати аварію, зупинити конвеєр і зберегти ресурси системи недоторканими.

---

## 20.1. Патерн герметичного відсіку

Уявіть себе капітаном підводного човна, який зазнав торпедної атаки. У правому борту утворилася пробоїна, вода хлинула у відсік №3. Що ви зробите?

Ви не будете намагатися тримати двері відчиненими й паралельно відкачувати воду, сподіваючись, що екіпаж зможе продовжувати обід у кают-компанії. Ви негайно віддасте наказ: *«Герметизувати відсіки!»*. Важкі сталеві перебірки з гуркотом зачиняються. Відсік №3 повністю ізолюється від решти корабля. Так, ми тимчасово втратили доступ до частини обладнання, але ми врятували підводний човен від затоплення.

Це і є філософія **Fail-Closed** у RadarPulse. Якщо під час валідації радарного батча (наприклад, через порушення контрольної суми) або в процесі обчислень на фоновому потоці виникає критичний виняток, система не намагається «згладити кути». Вона діє за протоколом екстреної евакуації:

1. **Блокування входу (Closing the Queue):**
   Черга Durable Queue негайно припиняє приймати нові батчі від провайдера. Будь-які спроби надіслати нові дані відхиляються на вході з повідомленням про помилку. Шлюз зачинено.
2. **Миттєве скасування воркерів (Cancelling Active Workers):**
   Усі паралельні воркери, які в цей момент обробляли інші батчі на фонових потоках, отримують сигнал скасування через загальний `CancellationToken`. Процесорний час більше не витрачається на обчислення, результати яких все одно не зможуть бути записані в ядро.
3. **Аварійне очищення буферів (Returning Buffers Cleanly):**
   Це найважливіша детективна складова. Оскільки воркери орендували великі масиви байтів та подій із загального пулу (`ArrayPool`), при виникненні аварії виникає величезний ризик витоку пам'яті. Система перехоплює виняток і гарантовано, у блоці `finally`, викликає методи `Dispose` для всіх активних дельт та конвертів. Всі орендовані буфери очищаються і повертаються в пул. Метрика накопиченого тиску пам'яті (Combined Retained Pressure) повертається строго до нуля.
4. **Фіксація стану аварії (Readiness State):**
   Система фіксує точний опис помилки, номер sequence, який став причиною аварії, та переводить загальний статус готовності системи `Readiness` в положення `false`.

---

## 20.2. Чому ми вбили автоматичний відкат (No Silent Fallback)

Під час обговорення архітектури віхи `023` виникла спокуса реалізувати механізм «м'якого відновлення» — автоматичного переходу на спрощений режим обробки (наприклад, запозичений режим `Borrowed Memory Fallback` без використання пулу орендованих буферів) у разі виникнення помилок у черзі.

Детективи RadarPulse категорично відкинули цю пропозицію:

```text
[Збій валідації батча]
         │
         ├──► [Автоматичний відкат (Silent Fallback)] ──► Приховування помилки, корупція даних! (REJECTED)
         │
         └──► [Аварійне блокування (Fail-Closed)] ─────► Зупинка, збереження доказів, безпека! (ACCEPTED)
```

Чому автоматичний безшумний відкат є злом?
* **Приховування доказів:** Якщо система тихо переключиться на альтернативний режим обробки, розробник ніколи не дізнається, що в основному конвеєрі стався збій. Помилка маскується, і розслідування стає неможливим.
* **Каскадне руйнування:** Намагаючись обійти помилку, система може продовжити записувати некоректно обчислені дані downstream-споживачам, що призведе до лавиноподібного зараження всієї аналітичної бази.

Наше залізне правило: **якщо сталася помилка валідації черги, вона має залишатися видимою і призводити до повної зупинки.** Жодних прихованих компромісів.

---

## 20.3. Бездоганне очищення пам'яті

Давайте подивимося, як концепція Fail-Closed виглядає на рівні метрик у наших тестах. Під час верифікації випуску Release Gate для віх `021`, `022` та `023` ми запускали спеціальні сценарії збоїв:

* Ми навмисно подавали на вхід пошкоджений батч із некоректною контрольною сумою.
* Система успішно виявляла підробку на етапі валідації.
* Черга негайно переходила у статус блокування (`Readiness = false`).
* Пізніші активні завдання, які вже крутилися у воркерів, маркувалися статусом `SkippedAfterFault` або `Canceled`.
* Всі ресурси вивільнялися.

Головний критерій успіху тесту: **термінальний комбінований тиск пам'яті (terminal combined retained pressure) наприкінці сесії аварійної зупинки мав дорівнювати рівно 0 байтів.**

Жоден байт орендованої пам'яті не повинен «зависнути» в купі. Кожен масив повернувся до свого пулу. Це доводить, що патерн Fail-Closed у RadarPulse захищає систему не лише від логічного хаосу, а й від фізичного виснаження ресурсів комп'ютера.
---

## 🔍 Матеріали справи (Investigation Case Files)

### 1. Вердикт детективів (Decision Trace & Rationale)
Реалізація принципу безпечного відключення — **Fail-Closed** (Віха `023`). При виявленні першого зламаного пакету черга негайно блокується, але всі активні воркери отримують сигнал завершити поточні завдання та коректно повернути буфери пам'яті в пули, мінімізуючи втрату даних.

### 2. Закони фізики рантайму (System Invariants)
* **Retained Pressure при зупинці**: Кінцевий тиск утримуваної пам'яті наприкінці аварійної сесії має дорівнювати рівно 0 байтів.
* **Блокування прийому**: Будь-який новий батч відхиляється, як тільки встановлено статус несправності черги.

### 3. Патологоанатомічний звіт (Failure Modes & Recovery)
* **Критичний виняток**: При генерації `FatalPipelineException` система закриває вхідні канали, зливає активних воркерів та переходить в захищений офлайн-режим.

### 4. Слід доказової бази (Implementation & Tests)
* Тести аварійного завершення: [RadarProcessingProductionPipelineFallbackTests.cs](../../../tests/RadarPulse.Tests/Processing/ProductPipeline/RadarProcessingProductionPipelineFallbackTests.cs)
* Тести відновлення та зливу: [RadarProcessingProductionPipelineRecoveryTests.cs](../../../tests/RadarPulse.Tests/Processing/ProductPipeline/RadarProcessingProductionPipelineRecoveryTests.cs)

### 5. Протокол допиту процесу (Verification Commands)
Запуск верифікації сценаріїв безпечного аварійного відключення:
```bash
dotnet test tests/RadarPulse.Tests/RadarPulse.Tests.csproj --filter "FullyQualifiedName~ProductionPipelineFallbackTests"
```
