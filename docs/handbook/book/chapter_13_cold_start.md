# Розділ 13: Прогрів холодного двигуна, або Справа про замерзлий патруль

Уявіть собі зимовий ранок в Оклахомі. Температура впала нижче нуля, вітер свистить у кронах дерев. Раптом надходить терміновий виклик: підозрюваний намагається втекти на автівці. Детектив вибігає з дільниці, стрибає в патрульний автомобіль, повертає ключ запалювання — але замерзлий двигун лише важко чхає, відмовляючись запускатися. Ми втрачаємо дорогоцінні хвилини, паливо згорає неефективно, а деталі двигуна зазнають колосального зносу під час холодного старту.

Досвідчений патрульний знає: взимку двигуни автомобілів потрібно прогрівати заздалегідь, тримаючи їх у стані готовності до миттєвого старту — у так званій **Prewarm Posture (поставі попереднього прогріву)**.

З аналогічною інженерною загадкою ми зіткнулися під час фінального шліфування продуктивності RadarPulse в межах Milestones 015-017.

## 13.1. Аномалія холодного старту: ArrayPool Misses

Коли ми успішно впровадили пулінг масивів `pooled-copy` (описаний у Розділі 12), наші великі тривалі тести показували майже нульове виділення нової пам'яті. Але коли ми почали запускати точкові тести окремих файлів за допомогою методу `MeasureFile()`, звіт про виділення пам'яті знову показав дивні цифри:

> [!WARNING]
> Перший запуск тесту над репрезентативним файлом KTLX виділив цілих **138 мегабайт** (138,151,728 байт) пам'яті, хоча всі наступні повторення цього ж тесту вкладалися в скромні ліміти.

Звідки взялися ці 138 мегабайт на «чистому» пулі?

Детектив почав детальне вивчення поведінки внутрішніх механізмів пам'яті. Звинувачення було пред'явлено так званим **промахам пулу (pool misses)**.

Коли програма запускається, її внутрішні пули `ArrayPool` абсолютно порожні. Вони не мають жодного готового масиву «на полицях». Коли наш метод `ToOwnedSnapshot()` приходить орендувати перший великий масив під події чи корисні дані, пул каже: «Вибач, у мене зараз нічого немає. Зачекай, я звернуся до операційної системи й створю для тебе новий масив».

Пул виділяє пам'ять у купі (allocation), віддає масив системі, а після завершення обробки приймає його назад і кладе на полицю. Наступні запити обслуговуються вже миттєво і без виділень — пул бере масив зі своєї полиці.

Але цей перший, «холодний» крок повністю псує статистику нашого бенчмарку. Він створює un-amortized cost — одноразову ціну холодного запуску, яка для коротких операцій (як-от обробка одного файлу) виглядає як величезний провал у продуктивності.

## 13.2. Рішення: Постава прогріву (Prewarm Posture)

Щоб зробити вимірювання чесними та прибрати ефект холодного старту з вимірюваних операцій, ми розробили концепцію **Prewarm Posture** (Попереднього прогріву).

Ми ввели явні контракти прогріву для наших ключових сховищ пам'яті:
* `RadarProcessingRetainedEventArrayPool.Prewarm()`
* `RadarProcessingRetainedPayloadByteArrayPool.Prewarm()`
* `RadarProcessingRetainedPayloadFactory.Prewarm()`

Перед тим як запустити секундомір бенчмарку та почати вимірювання операції `MeasureFile()`, система виконує фазу прогріву. Вона заздалегідь орендує великі масиви з пулу та негайно повертає їх назад, тим самим змушуючи пул ініціалізувати свої внутрішні полиці та заповнити їх структурами потрібного розміру.

Ми стандартизували розмір прогріву під репрезентативний радарний пакет даних:
* **Кількість подій (Event Count):** 65,536 об'єктів подій.
* **Об'єм байтів навантаження (Payload Bytes):** 67,108,864 байт (рівно 67 мегабайт).
* **Кількість батчів (Retained Batch Count):** 1.

Це забезпечило ідеальне налаштування пулу. Коли починається реальне вимірювання обробки файлу, пул уже має на полицях масиви потрібного об'єму і миттєво віддає їх воркерам.

## 13.3. Результати прогріву: Повний контроль над пам'яттю

Впровадження попереднього прогріву дало блискучі результати на наших Release-тестах:

* **Без прогріву (natural cold start):** 138,151,728 байт виділеної пам'яті.
* **З прогрівом (prewarmed start):** **68,420,960 байт** виділеної пам'яті (близько 68 МБ).
* **Порівняння з оракулом:** відношення обсягу пам'яті нашої системи до еталонного синхронного оракула (`BlockingBorrowed`) склало від **0.969x до 1.026x** (фактично повний паритет!).
* **Промахи пулу:** лічильник промахів пулу (`retained pool misses`) під час самого вимірювання показав рівно **0**.

Ми повністю вивели вартість ініціалізації за межі вимірюваного контуру. Патрульна машина виїжджає на переслідування з уже гарячим, прогрітим двигуном, демонструючи максимальне прискорення з першої ж секунди.

Усі валідаційні перевірки, проведені на сукупному наборі з 16 тестових груп та 28 пар безпеки, підтвердили стабільність цього рішення. Усі контрольні суми зійшлися, а лічильник помилок вивільнення ресурсів (`release failures`) залишився на нулі.

## 13.4. Висновки нашого великого розслідування

Chapters 7-13 стали хронікою нашої тривалої боротьби за продуктивність та асинхронність на лабораторному столі RadarPulse:

1. Ми почали з проблеми гарячого радара KTLX під час шторму (**Розділ 7**).
2. Написали динамічний ребаланс для перенесення розділів між шардами на межах батчів (**Розділ 8**).
3. Додали запобіжники проти паніки, обмежили телеметрію та ввели ліміти бюджету міграцій (**Розділ 9**).
4. Перейшли на фонових воркерів з обмеженими поштовими скриньками (**Розділ 10**).
5. Зіткнулися з катастрофою 9.95 ГБ сміття при спробі глибокого копіювання знімків (**Розділ 11**).
6. Перемогли сміття за допомогою стратегії багаторазової оренди масивів `pooled-copy` та пулінгу, знизивши виділення пам'яті на 98.97% (**Розділ 12**).
7. Захистили систему від аномалії холодного старту через механізм попереднього прогріву пулів (**Розділ 13**).

Тепер наш двигун на верстаку зібраний, налаштований та здатний обробляти мільярди подій з радарів NEXRAD з максимальною швидкістю та мінімальними накладними витратами. Але у великому місті завжди залишається місце для нових викликів — і в наступних розділах ми розслідуємо, як наш впорядкований двигун поводиться в умовах concurrency chaos (хаосу конкурентного доступу).
---

## 🔍 Матеріали справи (Investigation Case Files)

### 1. Вердикт детективів (Decision Trace & Rationale)
Рішення про впровадження прогріву пулів — `Prewarm Posture` (Віхи `015`-`017`). Холодний старт призводив до алокації 138 МБ на першому файлі через промахи пулу. Попередній примусовий розігрів пулів при старті знизив стартові алокації до контрольованих 68 МБ.

### 2. Закони фізики рантайму (System Invariants)
* **Prewarm Sizing**: На старті системи виділяється та нагрівається рівно 1 батч (`65_536` подій, `67_108_864` байт).
* **Однократність**: Прогрів виконується строго один раз перед початком прийому реальних радарних даних.

### 3. Патологоанатомічний звіт (Failure Modes & Recovery)
* **Пропуск прогріву**: Якщо прогрів відключений, перші оброблені батчі зазнають значних затримок у часі виконання (Latency Spikes) через фізичне виділення пам'яті ОС.

### 4. Слід доказової бази (Implementation & Tests)
* Метод прогріву: [RadarProcessingRetainedPayloadFactory.Prewarm.cs](../../../src/Infrastructure/Processing/Retention/Services/RadarProcessingRetainedPayloadFactory/RadarProcessingRetainedPayloadFactory.Prewarm.cs)
* Тести холодного старту: [RadarProcessingArchiveQueuedOverlapRunnerTests.StartupPrewarm.cs](../../../tests/RadarPulse.Tests/Processing/Queueing/RadarProcessingArchiveQueuedOverlapRunnerTests/RadarProcessingArchiveQueuedOverlapRunnerTests.StartupPrewarm.cs)

### 5. Протокол допиту процесу (Verification Commands)
Запуск тестування фази прогріву пулів пам'яті:
```bash
dotnet test tests/RadarPulse.Tests/RadarPulse.Tests.csproj --filter "FullyQualifiedName~StartupPrewarm"
```
