# Розділ 17: Свіжі карти на ходу (перерахунок застарілої топології, Stale Topology Recompute)

Паралельні обчислення дельт стають по-справжньому драматичними, коли на сцену виходить динамічний ребаланс шардів. У розподілених радарних мережах навантаження на обробники змінюється щомиті: грозовий фронт зміщується з однієї географічної зони в іншу, перевантажуючи одні обчислювальні шарди та залишаючи без діла інші.

Щоб оптимізувати роботу, RadarPulse використовує динамічну топологію розподілу джерел. Але коли ви намагаєтеся одночасно міняти карту доріг і запускати по ній швидкісні боліди-воркери, ви обов'язково отримаєте аварію. Якщо, звісно, у вас немає спеціального архітектурного детектора застарілих карт — **перерахунок застарілої топології (Stale Topology Recompute)** (впровадженого під час віхи `022`).

---

## 17.1. Проблема застарілих навігаторів

Уявіть ситуацію з життя детективів. Слідча група виїздить на оперативне завдання за адресою, вказаною в орієнтуванні. Вони використовують офіційну карту міста версії 10 (наша версія топології — `TopologyVersion = 10`). Шлях прокладено, екіпаж вирушив.

Але поки вони їхали, інший відділ поліції успішно завершив попередню справу, зафіксував зміни у міській інфраструктурі й перекрив кілька вулиць, зробивши їх односторонніми. Карта міста офіційно оновилася до версії 11.

Коли наш перший екіпаж прибуває на місце події та намагається здійснити затримання (зафіксувати результати в базі), виявляється, що вони заїхали під «цеглу» і порушили правила дорожнього руху. Їхні дії нелегітимні, оскільки вони орієнтувалися на застарілу версію карти.

У світі RadarPulse це виглядає так:

1. Воркер №3 бере радарний батч №3 і прокладає маршрут розподілу подій (`Route`) на основі поточної топології версії N. Він запускає розрахунок дельти на фоновому потоці.
2. У цей же час Воркер №2 завершує роботу над батчем №2 трохи раніше. Оскільки цей батч містив серйозне навантаження, його фіксація в ядрі запускає тригер ребалансу. Система перерозподіляє шарди й офіційно переводить топологію ядра на версію N+1.
3. Коли Воркер №3 нарешті приносить свою дельту до Координатора для фіксації, той звіряє версії: дельта була порахована для топології N, а поточний стан ядра вимагає топологію N+1.

Якщо просто записати цю дельту в ядро, ми отримаємо пошкодження узгодженості розподілу даних: події радарних джерел будуть записані в застарілі шарди, порушаться індекси адресації, а система втратить математичну узгодженість. Це злочин, який ми не можемо допустити.

---

## 17.2. Запобіжник: перерахунок застарілої топології

Для вирішення цієї проблеми під час віхи `022` ми впровадили механізм виявлення та перерахунку застарілих дельт. Замість того, щоб блокувати систему або записувати некоректні дані, ми діємо рішуче:

* **Детекція розбіжностей:** Під час спроби фіксації Координатор порівнює `Route.TopologyVersion` дельти з поточною версією топології сесії. Якщо версії збігаються — дельта записується миттєво. Якщо ні — оголошується тривога.
* **Знищення застарілого доказу:** Застаріла дельта безжально викидається. Викликається метод `Dispose`, який повертає орендовані масиви в `ArrayPool`, щоб не допустити витоку пам'яті.
* **Повторний забіг:** Оскільки система зберігає оригінальний радарний батч у буфері черги, вона створює новий маршрут обробки (`Route`) на базі вже нової, актуальної топології N+1. Батч повторно відправляється на обробку воркерам.
* **Чиста фіксація:** Після того, як воркери перерахували батч за новими правилами, свіжа дельта успішно проходить валідацію топології та фіксується в ядрі.

У коді це не виглядає як драматична сцена з мапами. Це короткий, жорсткий цикл рішення:

```text
+--------------------------------------------------+
| Дельта порахована на топології N                 |
+--------------------------+-----------------------+
                           |
                           v
+--------------------------------------------------+
| На фіксації топологія все ще N?                  |
+--------------------------+-----------------------+
                           |
               +-----------+-----------+
               |                       |
              Так                     Ні
               |                       |
               v                       v
+---------------------------+   +----------------------------+
| Застосувати дельту через  |   | Утилізувати застарілу      |
| впорядковану фіксацію     |   | дельту і повернути масиви  |
+-------------+-------------+   | в ArrayPool                |
              |                 +-------------+--------------+
              v                               |
+---------------------------+                 v
| Опублікувати              |   +----------------------------+
| детермінований результат  |   | Прокласти маршрут          |
+---------------------------+   | оригінального батча на     |
                                | топології N+1              |
                                +-------------+--------------+
                                              |
                                              v
                                +----------------------------+
                                | Перерахувати дельту        |
                                | і знову перевірити         |
                                | версію на фіксації         |
                                +----------------------------+
```

Цей підхід зберігає топологічні інваріанти системи в момент фіксації: результат застосовується лише тоді, коли його карта світу збігається з реальною картою ядра.

---

## 17.3. Ціна точності: метрики та продуктивність

Звісно, за все в цьому житті доводиться платити. Повторна обробка радарних батчів через застарілі карти топології — це додаткове навантаження на процесор та пам'ять.

Під час проведення стрес-тестів під час віхи `022` за допомогою спеціального інструменту порівняльного аналізу `rebalance-synthetic --mode ordered-rebalance` ми зафіксували точні цифри цього впливу.

Навантаження тесту виглядало так:
* Кількість ітерацій: **2 000**
* Батчів на ітерацію: **16**
* Загальна кількість логічних батчів: **32 000**
* Кількість прийнятих змін топології: **2 000**

Коли ми порівняли паралельну обробку з лімітом у 4 активні батчі (`ActiveBatches = 4`) проти послідовного базового сценарію (`ActiveBatches = 1`), детективні метрики показали наступне:

1. **Зростання кількості запусків воркерів:**
   Для обробки 32 000 логічних батчів система фактично здійснила **39 292** запуски воркерів! Це означає, що **7 292** батчі були обчислені повторно через те, що їхня топологія встигла застаріти, поки вони перебували в польоті на фонових потоках.
2. **Накладні витрати на виділення пам'яті:**
   Коефіцієнт виділення пам'яті зріс до **1.137x** порівняно з послідовним виконанням. Це прямий наслідок повторної маршрутизації батчів і перерахунків дельт.
3. **Загальний виграш у часі:**
   Незважаючи на тисячі повторних обчислень, загальний час виконання всього тесту склав **0.891x** від послідовного часу обробки! Тобто, ми отримали чисте **прискорення обробки на ~11%**.

У цьому синтетичному тесті паралелізм переміг навіть із урахуванням повторної роботи. Вільні процесорні ядра встигали виконати додаткові перерахунки швидше, ніж послідовний режим чекав би завершення кожного кроку перед наступним.

## 17.4. Гарячий шлях перерахунку

Один із практичних факторів, який утримав накладні витрати повторного обчислювального шляху в прийнятних межах, — локальність даних у пам'яті.

Коли батч відправляється на повторний розрахунок у цьому контурі, система не читає джерело заново. Вона повторно використовує вже прийнятий у чергу батч і його структури [`RadarStreamEvent`](../../../src/Domain/Streaming/Streams/Models/RadarStreamEvent.cs), будує для них новий маршрут на актуальній топології та рахує нову дельту. Тому ціна перерахунку — це не повний повтор усього шляху прийому даних, а додаткова маршрутизація й обчислення дельти над уже доступним матеріалом.

Головний висновок цього розслідування: механізм **перерахунок застарілої топології (Stale Topology Recompute)** є життєздатним і виміряним. Він дозволяє поєднувати гнучкість динамічного ребалансу з паралельним обробником, не підміняючи коректність оптимістичною вірою в те, що стара топологія “майже така сама”.

---

## 🔍 Матеріали справи

### 1. Вердикт детективів і причина рішення
Впровадження стратегії `перерахунок застарілої топології (Stale Topology Recompute)` (Віха `022`). Якщо під час асинхронного розрахунку батча відбулася зміна топології (ребалансування шардів), обчислена дельта вважається застарілою. Система автоматично відкидає її та перераховує на льоту перед фінальною фіксацією.

#### Чому застарілий маршрут треба рахувати заново
Коли топологія змінюється під час обчислення, можна було заблокувати всі міграції до завершення активних батчів. Це безпечно, але робить ребаланс повільним під реальним тиском. Можна було приймати стару дельту й сподіватися, що різниця несуттєва, але це вже не інженерія, а азарт. Ми обрали детекцію застарілої топології та перерахунок: якщо карта змінилася, старий розрахунок не фіксується. Ціна вибору — додаткові запуски воркерів і виміряна ціна алокацій; виграш — коректність сильніша за поспіх.

### 2. Закони фізики рантайму
* **Збіг версії топології**: [`TopologyVersion`](../../../src/Domain/Processing/Topology/Models/RadarProcessingTopologyVersion.cs) дельти на момент фіксації має строго дорівнювати поточній версії ядра.
* **Ціна перерахунку**: Збільшення кількості запусків воркерів з 32 000 до 39 292 та зростання алокацій на 1.137x під час активної зміни топології.

### 3. Патологоанатомічний звіт
* **Атака застарілих даних**: Якщо топологія оновлюється занадто швидко, система відкидає застарілі дельти і запускає їх перерахунок, стабілізуючи стан ядра перед фіксацією.

### 4. Докази продуктивності

| Твердження | Доказ | Де дивитися |
| :--- | :--- | :--- |
| Перекриття активних батчів дало виграш навіть під частими змінами топології | Матриця вузького місця обробки: `active=4`, відношення часу виконання `0.891x` проти того самого шляху з `active=1`, прийняті міграції `2_000 vs 2_000`, невдалих міграцій `0` | [022-ordered-rebalance-topology-commit-processing-bottleneck-performance-matrix.md](../../milestones/022-ordered-rebalance-topology-commit-processing-bottleneck-performance-matrix.md) |
| Перерахунок був реальною роботою, а не декоративним прапорцем | Матриця й закриття віхи фіксують `39_292` запусків воркерів для `32_000` логічних батчів та коефіцієнт алокацій `1.137x` | [022-ordered-rebalance-topology-commit-closeout.md](../../milestones/022-ordered-rebalance-topology-commit-closeout.md) |
| Живий контур впорядкованого ребалансу (`ordered-rebalance`) перераховує пізнішу активну дельту після міграції | Тест `OrderedConcurrentRebalanceRecomputesLaterActiveDeltaAfterTopologyMove` перевіряє, що другий батч фіксується вже на `Initial.Next()` | [RadarProcessingQueuedRebalanceSessionTests.OrderedConcurrentReference.cs](../../../tests/RadarPulse.Tests/Processing/Rebalance/RadarProcessingQueuedRebalanceSessionTests/RadarProcessingQueuedRebalanceSessionTests.OrderedConcurrentReference.cs) |
| Durable-контур відновлення також перераховує завершену застарілу дельту | Тест `DurableRebalanceRecomputesLaterCompletedDeltaAfterTopologyMove` перевіряє повторне програвання та порядок застосування стану топології | [RadarProcessingDurableRebalanceSessionTests.TopologyReplay.cs](../../../tests/RadarPulse.Tests/Processing/Rebalance/RadarProcessingDurableRebalanceSessionTests/RadarProcessingDurableRebalanceSessionTests.TopologyReplay.cs) |

### 5. Слід доказової бази
* Перерахунок у черговому контурі: [RadarProcessingQueuedRebalanceSession.Models.cs](../../../src/Infrastructure/Processing/Queueing/Services/RadarProcessingQueuedRebalanceSession/RadarProcessingQueuedRebalanceSession.Models.cs)
* Durable-перерахунок: [RadarProcessingDurableRebalanceSession.Models.cs](../../../src/Infrastructure/Processing/Durable/Services/RadarProcessingDurableRebalanceSession/RadarProcessingDurableRebalanceSession.Models.cs)
* Тест живого ordered-rebalance перерахунку: [RadarProcessingQueuedRebalanceSessionTests.OrderedConcurrentReference.cs](../../../tests/RadarPulse.Tests/Processing/Rebalance/RadarProcessingQueuedRebalanceSessionTests/RadarProcessingQueuedRebalanceSessionTests.OrderedConcurrentReference.cs)
* Тест durable-відновлення: [RadarProcessingDurableRebalanceSessionTests.TopologyReplay.cs](../../../tests/RadarPulse.Tests/Processing/Rebalance/RadarProcessingDurableRebalanceSessionTests/RadarProcessingDurableRebalanceSessionTests.TopologyReplay.cs)
* Трасування рішення і закриття віхи: [022-ordered-rebalance-topology-commit-decision-trace.md](../../milestones/022-ordered-rebalance-topology-commit-decision-trace.md), [022-ordered-rebalance-topology-commit-closeout.md](../../milestones/022-ordered-rebalance-topology-commit-closeout.md)

### 6. Протокол допиту процесу
Запуск тестування відновлення та перерахунку топологій:
```bash
dotnet test tests/RadarPulse.Tests/RadarPulse.Tests.csproj --filter "FullyQualifiedName~OrderedConcurrentRebalanceRecomputesLaterActiveDeltaAfterTopologyMove"
dotnet test tests/RadarPulse.Tests/RadarPulse.Tests.csproj --filter "FullyQualifiedName~RadarProcessingDurableRebalanceSessionTests"
```
